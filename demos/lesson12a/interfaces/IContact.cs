// naming convention is to start interface names with 'I'
interface IContact
{
    // best practice > keep interfaces short
    string ShowContactInfo(); // If a class is a contact, it must show their info
}