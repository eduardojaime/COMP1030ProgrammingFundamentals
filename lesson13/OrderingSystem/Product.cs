// 2.b composition question, create Product then Order
public class Product
{
    // + Code : string
    // translates to
    public string? Code;
    // NOT REQUIRED >> ? indicates the fields is nullable, meaning you are aware and expect null values
    public string Name;
}